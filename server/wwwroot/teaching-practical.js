// Worked local updates and executable experiments. Supplied update scores are
// explicitly separated from measured finite-element results.
const p=s=>`<p>${s}</p>`,h=s=>`<h2>${s}</h2>`,eq=s=>`<div class="theory-equation" role="math">${s}</div>`;
const steps=a=>`<ol class="algorithm">${a.map(s=>`<li>${s}</li>`).join('')}</ol>`;
const table=(head,rows)=>`<div class="theory-table"><table><thead><tr>${head.map(x=>`<th scope="col">${x}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${r.map(x=>`<td>${x}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;
const cite=(url,title)=>`<a class="inline-source" href="${url}" target="_blank" rel="noopener noreferrer">${title}</a>`;
const simp=cite('https://doi.org/10.1007/s00158-010-0594-7','Andreassen et al., 2011, §2–3');
const beso=cite('https://www.aeromech.usyd.edu.au/WCSMO2015/papers/1154_paper.pdf','Ghabraie, 2015, §5, Eqs. (6)–(7)');
const ls=cite('https://www.cmap.polytechnique.fr/~allaire/levelset_en.html','Allaire et al., level-set teaching code and manual');
const command='<pre class="teaching-command"><code>dotnet build server/TopTeach.csproj -c Release\ndotnet server/bin/Release/net9.0/TopTeach.dll --teaching-examples</code></pre>';
const dockerCommand='<pre class="teaching-command"><code>docker compose build beso\ndocker compose run --rm --no-deps beso --teaching-examples</code></pre>';
const scores=[['0',1,4,2,2.958333,2.479167,'✓'],['1',1,1,1,1.708333,1.354167,'—'],['2',1,2,2,2,2,'✓'],['3',0,0,6,1.25,3.625,'✓'],['4',1,1,1,1.104167,1.052083,'—'],['5',1,.5,.5,.729167,.614583,'—']];
export const practical={
1:{zh:h('张量、Voigt 向量与功共轭')+p('在二维中，ε(u) 是 2×2 对称张量，而 εV 是三个分量组成的工程应变向量。它们不能互换参与矩阵乘法。张量关系为 σ=𝔻:ε；矩阵关系为 σV=DpsεV，其中剪切分量的倍数已吸收到 εV。')+eq('σ:ε = σxxεxx+σyyεyy+2σxyεxy = σ<sub>V</sub>ᵀε<sub>V</sub>')+p('例如简单剪切 uₓ=ay、uᵧ=0 给出 εxy=a/2、γxy=a。令 G=E/[2(1+ν)]，则 τxy=Ga，而不是 Ga/2。三维也需成对使用相同顺序的应力向量与工程应变向量。')+eq('ε<sub>V,3D</sub>=[εxx, εyy, εzz, 2εxy, 2εyz, 2εzx]ᵀ'),
 en:h('Tensor, Voigt vector and work conjugacy')+p('In 2D, ε(u) is a symmetric 2×2 tensor whereas εV is a three-component engineering strain vector. They cannot be interchanged in matrix products. The tensor relation is σ=𝔻:ε; the matrix relation is σV=DpsεV, with the shear factor absorbed into εV.')+eq('σ:ε = σxxεxx+σyyεyy+2σxyεxy = σ<sub>V</sub>ᵀε<sub>V</sub>')+p('For simple shear uₓ=ay, uᵧ=0, εxy=a/2 and γxy=a. With G=E/[2(1+ν)], τxy=Ga, not Ga/2. In 3D the stress and engineering-strain vectors must likewise use matching component order.')+eq('ε<sub>V,3D</sub>=[εxx, εyy, εzz, 2εxy, 2εyz, 2εzx]ᵀ')},
4:{zh:h('算法 1：带密度滤波的 SIMP–OC')+p(`材料插值、OC 和密度滤波对应 ${simp}。以下步骤采用当前应用的设置；每步重新分析当前物理密度。`)+steps(['初始化 xⱼ=fᵥ，构造行归一化 W 和体积梯度 Wᵀv。','计算 ρ̃=Wx，装配 K(ρ̃)，在自由自由度上求解 Ku=f。','计算 C 及 ∂C/∂ρ̃ₑ，然后用 Wᵀ 得到 ∇ₓC。','给定移动限 m；对 λ 做二分搜索，试算截断后的 OC 更新，按 V(Wxⁿᵉʷ) 调整乘子区间。','接受 xⁿᵉʷ，重新分析其柔度；检查体积、设计变化及目标历史。达到迭代上限应单独报告。'])+
 h('数值例题：一次 OC 更新')+p('取四个等体积变量 x=[0.5,0.5,0.5,0.5]，W=I，vⱼ=1，V*=2，m=0.2。给定负梯度 −∇C=[8,2,8,2]。这些是用于隔离更新算子的输入，不声称来自某个悬臂梁状态。')+eq('V(xⁿᵉʷ)=√(8/λ)+√(2/λ)=2 &nbsp; ⇒ &nbsp; λ=4.5')+eq('xⁿᵉʷ=[2/3,1/3,2/3,1/3], &nbsp; Σxⁿᵉʷ=2')+p('移动限区间为 [0.3,0.7]，四个结果均在区间内，因此本例无需截断。单步最大变化为 1/6。该计算只验证给定梯度下的更新和体积，不证明实际非线性柔度必然下降。第 11 章命令可复算。'),
 en:h('Algorithm 1: density-filtered SIMP–OC')+p(`Interpolation, OC and density filtering follow ${simp}. The steps below use this application’s settings and solve the current physical-density state at each iteration.`)+steps(['Initialize xⱼ=fᵥ; construct row-normalized W and volume gradient Wᵀv.','Compute ρ̃=Wx, assemble K(ρ̃), and solve equilibrium on free DOFs.','Evaluate C and ∂C/∂ρ̃ₑ; apply Wᵀ to obtain ∇ₓC.','Fix move limit m; bisect λ, evaluate the clipped OC trial and adjust the bracket using V(Wxⁿᵉʷ).','Accept xⁿᵉʷ and reanalyze its compliance; check volume, design change and objective history. Report an iteration cap separately.'])+
 h('Worked example: one OC update')+p('Take four equal-volume variables x=[0.5,0.5,0.5,0.5], W=I, vⱼ=1, V*=2 and m=0.2. Supply −∇C=[8,2,8,2]. These inputs isolate the update operator; they are not claimed to be a cantilever state.')+eq('V(xⁿᵉʷ)=√(8/λ)+√(2/λ)=2 &nbsp; ⇒ &nbsp; λ=4.5')+eq('xⁿᵉʷ=[2/3,1/3,2/3,1/3], &nbsp; Σxⁿᵉʷ=2')+p('All four results lie inside the move interval [0.3,0.7], so clipping is inactive. The maximum change is 1/6. This checks the supplied-gradient update and volume, not descent of the actual nonlinear compliance. The Chapter 11 command reproduces the calculation.')},
6:{zh:h('算法 2：弱材料能量型 ESO')+steps(['初始化所有单元为实体，设置 ρmin=0.001 和计划体积分数 f₀=1。','按 Kₑ=ρₑᵖKₑ⁰ 求解平衡；在所有单元计算 αₑ=ρₑᵖ⁻¹Uₑ⁰，包括弱材料单元。','计算 α̂=Wα，令 fₖ₊₁=max(fᵥ,(1−ER)fₖ)，由计数公式确定保留数量。','仅对当前实体按 α̂ 排序，将最低分单元降为 ρmin；弱材料不得恢复。','重新分析更新后的设计。计划体积到达预算且无新删除时停止，报告这是删除过程结束。'])+p(`能量删除准则的理论背景见 ${cite('https://doi.org/10.1016/S0045-7825(02)00464-4','Tanskanen, 2002')}。上述弱材料、滤波及体积调度明确规定本应用的具体变体，不等同于原始应力拒绝比 ESO。`),
 en:h('Algorithm 2: weak-material energy ESO')+steps(['Initialize all elements solid, set ρmin=0.001 and scheduled fraction f₀=1.','Solve using Kₑ=ρₑᵖKₑ⁰; evaluate αₑ=ρₑᵖ⁻¹Uₑ⁰ over all cells, including weak material.','Compute α̂=Wα, schedule fₖ₊₁=max(fᵥ,(1−ER)fₖ), and determine the retained count.','Rank current solids only; lower the least sensitive to ρmin. Weak cells cannot return.','Reanalyze the updated design. Stop when the schedule reaches the budget and no further cell is removed; report completion of removal.'])+p(`The energy-removal background is discussed by ${cite('https://doi.org/10.1016/S0045-7825(02)00464-4','Tanskanen, 2002')}. Weak material, filtering and volume scheduling specify this application’s variant, not original stress-rejection ESO.`)},
7:{zh:h('算法 3：BESO 的分析、排序与恢复')+p(`Hard kill 的节点平均和距离权重参见 ${beso}。滤波归一化包含邻域内全部节点。`)+steps(['初始化全实体设计和体积调度；hard kill 另记录被动实体集合及最大加入率。','求解当前状态。Soft kill 保留 ρminᵖKₑ⁰；hard kill 仅装配实体并处理无效自由度，机构应报告失败。','Soft kill 对 ρᵖ⁻¹U⁰ 做空间滤波；hard kill 将实体 U⁰ 平均到节点，再外推到所有单元中心。','第一步直接使用滤波分数，以后与上一步已平均分数取算术平均。','Soft kill 用阈值满足体积调度；hard kill 先保留被动实体，再在当前实体和加入率允许的高分空单元间排序。','应用新材料状态，重新分析 C，记录体积与设计变化。体积目标、稳定性和迭代上限分别检查。'])+
 h('数值例题：六单元 hard-kill 排序')+p('采用 3×2 单位正方形网格，编号 e=2x+y，从 0 开始；上排为 [1,3,5]，下排为 [0,2,4]。输入实体状态和能量如下，R=1，无被动区。它是独立排序练习，不是由 FEA 求出的优化轨迹。')+table(['e','实体','U⁰','上一平均分数','当前滤波分数','新平均分数','保留'],scores)+p('节点 (1,1) 连接实体 0、1、2，故节点分数为 (4+1+2)/3=7/3。R=1 时每个单元仅包含四个等距角节点；例如单元 0 的滤波分数为 (4+2.5+3+7/3)/4=2.958333。')+p('本步保留 3 个单元，最大加入率为 1/6，允许恢复 ⌊6/6⌋=1 个单元。历史平均后最高三项为 3、0、2：删除 1、4、5，恢复 3，最终实体集合为 {0,2,3}。加入率控制的是恢复数量，并不要求最终设计连通。'),
 en:h('Algorithm 3: BESO analysis, ranking and recovery')+p(`Hard-kill nodal averaging and distance weights are described in ${beso}. Normalization includes all nodes inside the support.`)+steps(['Initialize solid material and the volume schedule; for hard kill also record passive solids and maximum admission.','Solve the current state. Soft kill retains ρminᵖKₑ⁰; hard kill assembles solids only and treats inactive DOFs. Report mechanisms as failures.','Soft kill spatially filters ρᵖ⁻¹U⁰. Hard kill averages solid U⁰ at nodes and extrapolates to every cell center.','Use the filtered score on the first iteration; later average it with the previous history-averaged score.','Soft kill uses a threshold for scheduled volume. Hard kill preserves passive solids, then ranks current solids and admission-eligible void cells.','Apply the new states, reanalyze C and record volume/change. Check target volume, stability and iteration limit separately.'])+
 h('Worked example: six-cell hard-kill ranking')+p('Use a 3×2 unit-square mesh with zero-based e=2x+y: upper row [1,3,5], lower row [0,2,4]. Supply the solid states and energies below, R=1 and no passive cells. This is an isolated ranking exercise, not an FE-generated optimization trajectory.')+table(['e','Solid','U⁰','Previous score','Filtered score','Averaged score','Keep'],scores)+p('Node (1,1) joins solids 0,1,2, giving (4+1+2)/3=7/3. With R=1 each cell has four equally weighted corner nodes. Cell 0 therefore receives (4+2.5+3+7/3)/4=2.958333.')+p('Retain three cells with maximum admission 1/6, allowing ⌊6/6⌋=1 recovered cell. After history averaging, the highest scores belong to 3,0,2. Remove 1,4,5 and restore 3: the final set is {0,2,3}. Admission limits recovered count; it does not enforce connectivity.')},
8:{zh:h('体积乘子的连续解释')+eq('dV/dt=∫Γ<sub>free</sub> Vₙ dΓ=0 &nbsp; ⇒ &nbsp; λ=∫Γ<sub>free</sub>(q−γκout)dΓ / |Γ<sub>free</sub>|')+p('该表达式只适用于前述光滑自由边界、均匀 L² 度量及瞬时恒体积条件。它使增加与删除的瞬时体积平衡；体积缩减阶段需另给目标变化率。当前应用以离散体积二分求 λ，不直接采用该边界积分。')+
 h('迎风离散与符号核对')+eq('D⁻φᵢ=φᵢ−φᵢ₋₁, &nbsp; D⁺φᵢ=φᵢ₊₁−φᵢ')+eq('G₊²=Σaxes max[min(D⁻φ,0)²,max(D⁺φ,0)²]')+eq('G₋²=Σaxes max[max(D⁻φ,0)²,min(D⁺φ,0)²]')+eq('φᵢⁿ⁺¹=φᵢⁿ+Δt Vᵢ G<sub>sign(Vᵢ)</sub>, &nbsp; Δt=τ/[d maxᵢ|Vᵢ|]')+p('差分使用单元边长为 1 的坐标，d 为维度，V=drive−λ。取 V=0 时不移动。应用中 τ 是归一化步长，不是物理时间；该 CFL 限制约束一阶 Hamilton–Jacobi 传输，含曲率的更新还需独立稳定性与验收检查。')+
 h('算法 4：当前固定网格 level-set 变体')+steps(['用规则孔洞初始化正内负外的 φ，并由平滑 Heaviside 得到分析密度。','求解线弹性状态；将实体侧单元能量延拓到空域，空间滤波并以界面带平均能量归一化。','加入与正内符号一致的曲率项。每五步从网格边的零交点重建距离，并平移 φ 以恢复原离散体积。','对候选乘子计算 CFL 步长和 Godunov 更新；二分乘子以满足本步可达的离散体积。','在目标体积附近重新分析候选设计；若柔度上升则缩短步长并重试。体积缩减阶段不要求柔度下降。','接受状态后检查体积、密度变化及目标历史；分别报告未接受步、迭代上限或停止判据。'])+p('距离重建是到离散零交点的最近距离，符号由旧场保留；它不是精确到连续曲面的距离，也不是重初始化 PDE 的求解。速度归一化后，界面正则参数不能直接解释为前述有量纲的周长乘子 γ。')+p(`经典传输、重初始化与速度正则化的背景见 ${ls}。这里明确列出应用变体，不将这些离散处理等同于连续下降证明。`),
 en:h('Continuum interpretation of the volume multiplier')+eq('dV/dt=∫Γ<sub>free</sub> Vₙ dΓ=0 &nbsp; ⇒ &nbsp; λ=∫Γ<sub>free</sub>(q−γκout)dΓ / |Γ<sub>free</sub>|')+p('This expression assumes the preceding smooth free boundary, uniform L² metric and instantaneous constant volume. It balances instantaneous addition and removal; volume reduction requires a prescribed rate instead. App determines λ from discrete-volume bisection, not this boundary integral.')+
 h('Upwind discretization and sign check')+eq('D⁻φᵢ=φᵢ−φᵢ₋₁, &nbsp; D⁺φᵢ=φᵢ₊₁−φᵢ')+eq('G₊²=Σaxes max[min(D⁻φ,0)²,max(D⁺φ,0)²]')+eq('G₋²=Σaxes max[max(D⁻φ,0)²,min(D⁺φ,0)²]')+eq('φᵢⁿ⁺¹=φᵢⁿ+Δt Vᵢ G<sub>sign(Vᵢ)</sub>, &nbsp; Δt=τ/[d maxᵢ|Vᵢ|]')+p('Differences use element-unit coordinates, d is dimension and V=drive−λ. Zero velocity causes no motion. The parameter τ is a normalized step, not physical time. This CFL bound controls first-order Hamilton–Jacobi transport; a curvature-dependent update still requires separate stability and acceptance checks.')+
 h('Algorithm 4: the implemented fixed-grid level-set variant')+steps(['Initialize seeded holes with positive-inside φ and map to analysis density through smooth Heaviside interpolation.','Solve elasticity, extend solid-side energy into void, spatially filter and normalize by mean interface-band energy.','Add curvature with the positive-inside sign. Every five iterations rebuild distance from grid-edge zero crossings, then shift φ to restore the previous discrete volume.','For trial multipliers evaluate CFL-limited Godunov updates; bisect the multiplier to match the reachable step volume.','Near target volume, reanalyze each trial and backtrack compliance-increasing steps. During volume reduction, decreasing compliance is not required.','Check accepted volume, density change and objective history. Distinguish rejected updates, iteration caps and stopping criteria.'])+p('Distance rebuilding uses nearest discrete zero crossings and the old sign. It is neither exact distance to a continuous surface nor a reinitialization PDE solve. After energy normalization, the interface regularization parameter is not directly the dimensional perimeter multiplier γ above.')+p(`Classical transport, reinitialization and regularization are described in ${ls}. These explicit implementation choices do not inherit the continuum descent proof automatically.`)},
10:{zh:h('可执行实验：固定设计、OC 与 BESO')+p('在项目根目录执行以下命令，需要项目既有的 .NET SDK 和本机求解器依赖。本机构建依赖见仓库 docs/DISTRIBUTION.md；该命令不会启动网页服务，并在贯穿例题中执行一次优化更新。')+command+p('使用 Docker 时无需在宿主机安装 .NET；在项目根目录运行：')+dockerCommand+p('输出 JSON 的基础部分包括：mechanics 是固定全实体悬臂的有限元结果；oc 和 beso 分别复算第 5、8 章给定输入下的局部更新。后两者不是完整优化的验证。12×8 与 12×8×2 算例均取 h=1、ν=0.30000001192092896，保持左端固定、右端中点节点力。')+table(['固定设计','C(E=1,F=−1)','C(E=1,F=−2)','C(E=2,F=−1)'],[['2D · 12×8','18.603604','74.414415','9.301802'],['3D · 12×8×2','9.941230','39.764920','4.970615']])+p('表内是当前求解器实测值，保留六位小数，不是外部文献基准。命令以相对比值检查 4 和 0.5，容差为 10⁻⁸；本机与不同平台的末位舍入可能不同。局部更新检查 OC 总体积为 2，BESO 保留集合为 {0,2,3}，失败时命令返回非零退出码。')+h('连续体收敛实验的附加条件')+p('如需研究连续体柔度极限，应另定义固定物理宽度的边界载荷带，保持其积分总力、支撑区域、材料尺度和设计表示一致；先在固定几何上用至少三个网格检查状态分析，再研究优化结果。当前 App 未提供该分布载荷入口，不能用节点载荷实验替代。'),
 en:h('Executable experiments: fixed design, OC and BESO')+p('Run the following from the repository root with the project’s .NET SDK and native-solver dependencies; see docs/DISTRIBUTION.md for native setup. This command does not start the web server; its end-to-end example executes one optimizer update.')+command+p('With Docker, no host .NET installation is needed; run from the repository root:')+dockerCommand+p('The JSON contains mechanics for fixed fully solid cantilevers, and oc/beso for the supplied-input updates in Chapters 5 and 8. The latter are not complete-optimizer validation. Meshes are 12×8 and 12×8×2, h=1, ν=0.30000001192092896, with a fixed left end and right-end midpoint nodal force.')+table(['Fixed design','C(E=1,F=−1)','C(E=1,F=−2)','C(E=2,F=−1)'],[['2D · 12×8','18.603604','74.414415','9.301802'],['3D · 12×8×2','9.941230','39.764920','4.970615']])+p('Values are measurements from the current solver rounded to six decimals, not external literature benchmarks. The command checks ratios 4 and 0.5 to 10⁻⁸; final rounding can vary across platforms. It also checks OC volume 2 and BESO set {0,2,3}, returning a nonzero exit code on failure.')+h('Additional conditions for continuum convergence')+p('To study a finite continuum compliance limit, prescribe a boundary load band of fixed physical width, retaining its resultant, support region, material scale and design representation. First check analysis on a fixed geometry with at least three meshes, then study optimization. App has no distributed-load input, so its nodal-load experiment cannot substitute for this protocol.')}
};

practical[9]={
 zh:h('显示变化率与停止判据')+eq('Δₖ=|Σⱼ₌ₖ₋₄ᵏ Cⱼ−Σⱼ₌ₖ₋₉ᵏ⁻⁵ Cⱼ| / max(|Σⱼ₌ₖ₋₉ᵏ⁻⁵ Cⱼ|,10⁻³⁰)')+p('这里 Cⱼ 是第 j 帧所显示设计重新分析后的柔度；前十帧不足时 Δ 显示为 1，表示窗口尚未建立。该显示指标不单独决定停止。求解器内部使用更新前状态的历史窗口，并结合各方法的体积、设计变化或删除结束条件，因此可能相差一个状态；达到迭代上限仍单独报告。')+h('离散预算的可行性')+p('ESO 对 n(f−ρmin)/(1−ρmin) 向下取整；hard-kill BESO 对 nf 向下取整。因此最终预算不会因整单元计数而被超过，允许少于一个材料增量的欠用。体积缩减阶段的计划上限仍可能高于最终预算。Hard kill 的被动实体超过最终允许数量时拒绝该设置；全空或不连通状态可能无法承载。Soft-kill BESO 的阈值与相同敏度可能产生体积偏差，必须查看实际体积，不能把其停止状态当作严格可行性证书。'),
 en:h('Displayed change and stopping criteria')+eq('Δₖ=|Σⱼ₌ₖ₋₄ᵏ Cⱼ−Σⱼ₌ₖ₋₉ᵏ⁻⁵ Cⱼ| / max(|Σⱼ₌ₖ₋₉ᵏ⁻⁵ Cⱼ|,10⁻³⁰)')+p('Cⱼ is the reanalyzed compliance of displayed frame j. Before ten frames exist, Δ is 1 as a window-not-ready sentinel. This display metric alone does not terminate optimization. Internal stopping uses pre-update history together with method-specific volume, design-change or removal-completion conditions, so its history can differ by one state. Iteration caps are reported separately.')+h('Discrete budget feasibility')+p('ESO rounds n(f−ρmin)/(1−ρmin) down; hard-kill BESO rounds nf down. Whole-cell counting therefore does not exceed the final budget, leaving less than one material increment unused. During reduction, the scheduled bound may still exceed the final budget. Hard kill rejects passive solids exceeding the final retained count; empty or disconnected designs may not carry load. Soft-kill BESO thresholds and tied scores can produce volume mismatch: inspect actual volume rather than treating termination as a strict feasibility certificate.')};
const workedLink='<a href="worked-cantilever.json" download>worked-cantilever.json</a>';
practical[10].zh+=h('贯穿例题：4×4 Q4 悬臂的一次 SIMP 更新')+p('取 L=H=4，单位厚度，E=1、ν≈0.3；左端固定，右端中点 Fy=−1。初始 x=0.5，p=3，η=10⁻⁹，r=1，故 W=I；移动限为 0.2，目标体积分数 0.5。此例只完成一次真实优化更新，不宣称已收敛。')+steps(['编号 node=5x+y、element=4x+y，位移自由度为 (2node,2node+1)。固定自由度 0–9，自由自由度 10–49；节点 22 的 y 方向承受 −1。','按第 3 章 Q4 积分与装配公式建立 50×50 刚度矩阵，取 40×40 自由子矩阵 Kff。求解 Kff uf=ff。','由外功计算初始 C=57.085138；使用第 5 章导数，单元 0 的 ∂C/∂x₀=−54.015505。','对体积乘子二分得到 λ=18.520425（柔度目标约定）。单元 0 的未截断更新约为 0.853894，截断为 0.7；全部更新后密度见数据文件。','重新装配并求解，得到 C=42.166652，体积分数约为 0.5。此步降幅是本次计算结果，不是 OC 任意步必降的保证。'])+p('配套命令的 worked 字段给出连接表、Kff、载荷、更新前后节点位移、全部梯度与密度。可下载本次计算的完整数据：'+workedLink+'。其中平衡残差约为 1.6×10⁻¹⁵；不同平台末位可能不同。');
practical[10].en+=h('End-to-end example: one SIMP step on a 4×4 Q4 cantilever')+p('Set L=H=4, unit thickness, E=1 and ν≈0.3. Fix the left edge and apply Fy=−1 at the right-edge midpoint. Initially x=0.5, p=3, η=10⁻⁹ and r=1, so W=I. The move limit is 0.2 and volume fraction is 0.5. This is one actual optimization update, not a converged design.')+steps(['Number nodes as 5x+y and elements as 4x+y; displacement DOFs are (2node,2node+1). Constrain DOFs 0–9 and retain 10–49. Apply −1 to the y DOF of node 22.','Use Chapter 3 quadrature and assembly to construct the 50×50 stiffness matrix and its 40×40 free block Kff. Solve Kff uf=ff.','External work gives initial C=57.085138. Chapter 5 differentiation gives ∂C/∂x₀=−54.015505 for element 0.','Bisection gives λ=18.520425 for the compliance objective. Element 0 has an unclipped update of approximately 0.853894, clipped to 0.7; the data file contains every updated density.','Reassemble and solve: C=42.166652 and volume fraction is approximately 0.5. Descent here is a measured result, not a guarantee for every OC step.'])+p('The command’s worked field contains connectivity, Kff, loads, initial and updated nodal displacements, gradients and densities. Download the complete recorded example: '+workedLink+'. The equilibrium residual is approximately 1.6×10⁻¹⁵; final digits can vary across platforms.');
